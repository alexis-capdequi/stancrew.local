<!DOCTYPE html>

<title>Edit Music</title>

{% block body %}
    <h1>Edit Music</h1>

    {{ include('admin/music/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_music_index') }}">back to list</a>

    {{ include('admin/music/_delete_form.html.twig') }}
{% endblock %}
